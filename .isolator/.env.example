# Claude Code — authenticate with your Pro/Max subscription instead of an API key.
# Generate the token on the host:  claude setup-token
# Put the REAL value in .isolator/.env (gitignored) — never in this file.
CLAUDE_CODE_OAUTH_TOKEN=

# GitHub token (optional) — for issue/PR operations inside the sandbox.
GH_TOKEN=
